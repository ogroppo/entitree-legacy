export default function universityByQid(id: Number) {
  if (!id) {
    return null;
  }
  // const entityId = id[0].value;
  const religion = null;
  if (!religion) {
    return null;
  }
  return religion;
}
